{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "g++ -o a.exe ${file} -std=c++17 -Wall --pedantic-errors -fexec-charset=cp932",
            "problemMatcher": "$tsc", 
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
